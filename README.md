Upload you gemini api key in line 368 of the file.
```    
const GEMINI_API_KEY = "PLACE KEY HERE";
```
The API key can be obtained from google ai studio and you **do not** need the premium one for this.

The Google audio preview model keeps changing too, so *you might need to* update that just below the API key also.
